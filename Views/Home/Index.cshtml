@model List<Contact>
@{
    ViewBag.Title = "My Contacts";
}
<h2>Contact List</h2>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Category</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var contact in Model)
        {
            if (contact != null)
            {
                <tr>
                    <td>
                        <a asp-controller="Contact" asp-action="Details"
                           asp-route-id="@contact.ContactId"
                           asp-route-slug="@contact.Slug.Replace(" ","-")">
                           @contact.FirstName @contact.LastName
                        </a>
                    </td>
                    <td>@contact.Email</td>
                    <td>@contact.Phone</td>
                    <td>@contact.Category.CategoryName</td>
                </tr>
            }
        }
    </tbody>
    <a asp-controller="Contact" asp-action="Add" class="btn btn-outline-success">Add New Contact</a>
</table>
